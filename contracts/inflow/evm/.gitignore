# Foundry build output
/out

# Foundry compilation cache
/cache

# Foundry coverage reports
/coverage

# Forge libraries
/lib

# Foundry script runs output
/broadcast

# Local secrets and per-environment deployment config. Deny by default so a new
# .env.<target> cannot be committed by accident; the example file is the only exception.
.env*
!.env.example
